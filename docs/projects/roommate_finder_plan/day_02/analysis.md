# Day 02: UI/UX Theming via AppConfig & Base Layouts

## Objectives
- Define the color palette and typography in `AppConfig`.
- Implement the `MainLayout` using `osmea_components`.
- Configure `SplashView` and `OnboardingView` from `masterfabric_core`.
- Ensure theme changes are reflected dynamically from the config file.
