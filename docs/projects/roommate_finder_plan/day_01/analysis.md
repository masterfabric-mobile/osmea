# Day 01: Project Setup, Architecture, AppConfig & Slang Init

## Objectives
- Initialize the Flutter project structure within `projects/roommate_finder`.
- Configure `masterfabric_core` and `osmea_components` dependencies.
- Set up `AppConfig` JSON structure for dynamic theming.
- Initialize `slang` for multi-language support.
- Create base folder structure (views, view_models, models, services).
